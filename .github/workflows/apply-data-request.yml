name: Apply data request feature
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Download and apply verified patch
        run: |
          set -e
          curl -sL "https://raw.githubusercontent.com/VegettoSan/PSVitaAlive/main/scripts/patch_data_request.py" -o /tmp/check.py || true
          # Inline minimal: apply from this repo after script is fixed below
          python3 -c "print('placeholder workflow')"
